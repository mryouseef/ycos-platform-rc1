import type{Metadata}from"next";import{PortalPage}from"@/src/portal/portal-page";export const metadata:Metadata={robots:{index:false,follow:false}};export default async function P({params}:{params:Promise<{locale:string}>}){const{locale}=await params;return <PortalPage locale={locale} resource="ADMIN" title={locale==="ar"?"إدارة المنصة الاصطناعية":"Synthetic platform administration"}/>}
