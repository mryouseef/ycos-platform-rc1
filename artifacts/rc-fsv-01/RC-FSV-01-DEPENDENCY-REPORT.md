# RC-FSV-01 Dependency Report

The clean RC package restored **97** production dependencies through the approved lockfile. `pnpm audit --prod --json` reported 0 info, low, moderate, high, and critical advisories. No provider SDK or external AI runtime was found in active PN/IAF/PXSI paths.
