# RC-FSV-01 Reproducibility Report

Clean extraction of the approved PXSI-01 package completed successfully. `pnpm install --frozen-lockfile`, TypeScript, and production build passed from `/tmp/rc-fsv-01-clean/PXSI-01`. The clean environment also ran the local PostgreSQL IAF-01 harness successfully. Evidence: `raw-evidence/dependency-restore.txt`, `typecheck.txt`, `build.txt`, and `clean-postgres-iaf01.txt`.
